# Bshell_to_check_storage
ldap to run a ldapsearch command, then saves the output in a log. If it fails, the script check log file to match the error code.
pscheck to check components state in log files.
